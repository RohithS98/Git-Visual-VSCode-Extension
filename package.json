{
    "name": "gitvisual",
    "displayName": "GitVisual",
    "description": "Visualize git history",
    "version": "0.0.1",
    "publisher": "RohithS98",
    "engines": {
        "vscode": "^1.23.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.sayHello"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.sayHello",
                "title": "Git Visual History V2"
            },
            {
                "command": "extension.vscexpress",
                "title": "Visual Button"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "npm run compile && node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "vscode": "^1.1.21",
        "tslint": "^5.8.0",
        "@types/node": "^8.10.25",
        "@types/mocha": "^2.2.42"
    },
    "dependencies": {
        "@types/express-serve-static-core": "^4.16.0",
        "animejs": "^2.2.0",
        "vscode-express": "^0.1.11"
    }
}
